You are an expert Python data scientist. Your task: Write a comprehensive Jupyter/Colab notebook that reads an Android GNSS Logger text file, parses its contents into pandas DataFrames for use in data analysis.

Provide the code in distinct, well-documented Colab cells. Each cell must start with a # @title comment so the user can easily minimize the cell and hide the code.

1. Context and Data Format
The input is a text file generated by the Android GNSS Logger app.
It contains metadata and column definitions in comment lines starting with #.
The column headers for data rows are defined in comment lines like # Raw,utcTimeMillis,... and # Fix,Provider,....
The actual data rows start with a message identifier string, such as Raw or Fix.
The fields may vary depending on the version of the GNSS Logger app, so you MUST dynamically extract the column names from the # Raw,... and # Fix,... header lines.
The fields in the log file are mostly numeric, but some are alpha fields.
For this task, only parse the Raw and Fix sentences. Ignore UncalAccel, Accel, UncalGyro, etc.
2. Required Colab Structure & Tasks
Cell 1: Setup and File Upload
Start the notebook with a markdown cell describing what the code does and how to use it.
Create a code cell: 
# @title Setup and File Upload
Import necessary libraries (e.g., pandas, plotly.express, folium, numpy, datetime).
Provide code using google.colab.files.upload() to prompt the user to upload the GNSS Logger file.

Cell 2: Data Parsing & Summary

# @title Parse Data and Print Summary
Parse the uploaded file into two pandas DataFrames: df_raw and df_fix.
CRITICAL ALIGNMENT INSTRUCTION: The header lines include the message identifier (e.g., 'Raw' or 'Fix') as the first column. When splitting the actual data rows, DO NOT skip the first element (the message identifier). You must keep the columns perfectly aligned with the headers.
Crucial Data Cleaning: Android GNSS logger files often have trailing commas on data lines that are not present in the header. Before converting your parsed lists into DataFrames, you MUST pad or truncate each row array to exactly match the length of the header array to prevent Pandas from throwing a ValueError.
Extract and print the header line with Version, Platform, and Manufacturer 
After parsing the DataFrames, print a clean summary report of what was read, formatted exactly like this: Successfully loaded %d Raw GNSS records, over %d seconds, from time hh:mm:ss to hh:mm:ss UTC
Note: In the Raw messages, values like BiasNanos, TimeNanos, and utcTimeMillis are identical for all records at the same epoch.

Cell 3: Data Augmentation (Mappings)

# @title Map Constellations and Frequencies
In df_raw, create a ConstellationName column by mapping the numeric ConstellationType to string names using this exact dictionary: {1: 'GPS', 2: 'SBAS', 3: 'GLONASS', 4: 'QZSS', 5: 'BDS', 6: 'Galileo', 7: 'IRNSS'}.
Map CarrierFrequencyHz to their standard band names (L1, L5, E1, E5a, etc.). Since CarrierFrequencyHz can have slight variations, use a tolerance-based mapping function that considers both the Constellation and the Frequency. Provide this exact function in the cell and apply it to create a new FrequencyBand column:
python
def get_band_name(constellation, freq_hz):
    if pd.isna(freq_hz): return 'Unknown'
    freq_mhz = freq_hz / 1e6
    if constellation == 'GPS' or constellation == 'QZSS':
        if abs(freq_mhz - 1575.42) < 5: return 'L1'
        if abs(freq_mhz - 1176.45) < 5: return 'L5'
        if abs(freq_mhz - 1227.60) < 5: return 'L2'
    elif constellation == 'Galileo':
        if abs(freq_mhz - 1575.42) < 5: return 'E1'
        if abs(freq_mhz - 1176.45) < 5: return 'E5a'
        if abs(freq_mhz - 1207.14) < 5: return 'E5b'
        if abs(freq_mhz - 1278.75) < 5: return 'E6'
    elif constellation == 'GLONASS':
        if 1598 < freq_mhz < 1606: return 'G1'
        if 1242 < freq_mhz < 1249: return 'G2'
    elif constellation == 'BDS':
        if abs(freq_mhz - 1561.098) < 5: return 'B1I'
        if abs(freq_mhz - 1575.42) < 5: return 'B1C'
        if abs(freq_mhz - 1176.45) < 5: return 'B2a'
        if abs(freq_mhz - 1207.14) < 5: return 'B2b'
        if abs(freq_mhz - 1268.52) < 5: return 'B3'
    return 'Other'
Do not plot by the raw numeric values in the subsequent plots; exclusively use the new ConstellationName and FrequencyBand string columns.
Ensure the code handles missing columns gracefully, as header fields can vary by logger version